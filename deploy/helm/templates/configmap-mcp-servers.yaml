apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-servers
  namespace: {{ .Values.agentNamespace }}
  labels:
    app.kubernetes.io/part-of: purko
data:
  servers: |
    {{- .Values.mcpServers | toYaml | nindent 4 }}
